#include <stdio.h>

int *mergesort(int *n, int size_n);
void merge(int *arr, int size_arr);

int main(){
    int n[] = {5, 2, 1, 3, 6, 4, 9, 0, 12, 13, 14, 11, 117};
    int len_of_n = sizeof(n)/sizeof(n[0]);

 
}

